# QuickBooks simulation Gatekeeper

Read-only access to the existing deterministic synthetic DEPARTURE accounting dataset. It provides company information, customer and service revenue analytics, and profit-and-loss reports without network access or credentials.

Every returned figure is simulated and must be labeled accordingly. Every read is logged as an observation.

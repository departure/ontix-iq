# Organization Gatekeeper

Auto-provisioned read-only access to the canonical DEPARTURE profile packaged from `ORGANIZATION.md`. It supports full-profile reads and focused line searches; every call records an observation.

Update the root `ORGANIZATION.md`, copy the canonical revision into `src/organization.md`, and run `pnpm types:check` before deployment. No credentials are required.
